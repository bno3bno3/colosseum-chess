# 斗兽棋：规则与架构设计

## 1. 产品目标

这是一个由一台电脑在局域网内托管的实时网页游戏。主机运行一个 Node.js 服务后，会显示本机和局域网访问地址；手机、平板或其他电脑通过同一个端口打开网页即可进入大厅。大厅可同时存在任意数量的房间，每个房间容纳两名玩家（真人或 AI）和不限数量的观战者。

界面以示例图为唯一视觉基准：竖屏舞台、顶部双方信息与中央倒计时、中央 4×8 棋盘、红蓝软方块棋子、两侧纵向血条、黄色功能按钮和高饱和霓虹背景。桌面端保持手机游戏的竖屏比例，移动端铺满可用高度。

## 2. 已确认的游戏规则

### 2.1 棋盘与棋子

- 棋盘为 4 列 × 8 行，共 32 格。
- 红、蓝双方各 16 枚棋子：2 大象、2 老虎、2 狼、2 狗、3 猫、3 老鼠、2 足球。
- 开局时 32 枚棋子由服务器使用安全随机数洗牌，全部背面朝上。
- 开局随机分配红、蓝阵营和先手方；玩家只能移动自己已经翻开的棋子，但双方都能翻任意暗子。
- 暗子不能移动、不能直接被普通棋子或足球吃掉，只能在当前回合执行“翻开”。

### 2.2 回合与移动

- 每回合只能执行一个动作：翻一枚暗子，或移动一枚己方明子（吃子也算移动）。
- 非足球棋子只能向上、下、左、右移动一格，不能斜走。
- 移入空格就是普通移动；移入敌方明子所在格就是尝试吃子。
- 不能移动到己方棋子或暗子所在格。
- 每回合限时 30 秒。超时由服务器自动过轮，不额外扣血。
- 玩家可随时认输；进行中的对局里主动退出房间按认输处理。意外断线保留座位 20 秒，期间同一浏览器可自动重连；超时仍未恢复则判负。

### 2.3 吃子关系

普通动物的强弱顺序为：大象 > 老虎 > 狼 > 狗 > 猫 > 老鼠；同级可以互吃。

- 大象可吃大象、老虎、狼、狗、猫，但不能吃老鼠。
- 老鼠可吃老鼠和大象，不能吃老虎、狼、狗、猫。
- 老虎、狼、狗、猫可吃同级及所有排在自己之后的较弱动物。
- 足球可被大象、老虎、狼、狗、猫吃掉，但老鼠不能吃足球。
- 足球吃子不比较大小：它能吃任意敌方明子（包括足球），但必须满足下述“隔子打击”条件。

### 2.4 足球规则

- 足球不吃子时，与普通棋子相同，只能正交移动一格到空格。
- 足球吃子时，起点与目标必须同行或同列。
- 起点和目标之间必须恰好存在一枚“炮架”；炮架可以是任意阵营、任意类型、明面或背面的棋子。
- 起点到炮架、炮架到目标之间都允许有零个或多个空格。
- 目标必须是敌方已经翻开的棋子。

### 2.5 重复局面限制

- “同一局面”定义为相同的 4×8 棋盘排列（暗子统一视为暗子，不比较棋子 ID）以及相同的行动方。
- 每个局面最多出现 3 次；任何会让某个局面第 4 次出现的移动都是非法移动，玩家必须变招。
- 服务器维护整局的局面出现次数并作权威校验；客户端同步标出禁行目标，AI 的合法动作和搜索状态也携带同一份历史。

### 2.6 血量与胜负

- 每方初始血量默认 14 格；房主可在双方准备前于大厅将其设为 1–16 格。
- 每当己方一枚棋子被吃，己方扣 1 格血，不区分棋子类型。
- 血量降至 0 时立即失败。
- 认输、进行中主动离房或断线超时也会失败。
- 对局结束后双方可以回到同一房间、取消准备并再次开始；每局都会重新随机阵营、先手和棋盘。

### 2.7 可选规则集

房主可在无人类玩家准备时逐项启用或禁用扩展规则；开局后规则集冻结，并写入棋局、AI 公开状态和历史记录。规则由 `server/rules/` 中彼此独立的模块注册，模块可修改棋子配比、攻击结果、吃子后效果和回合结束效果；新增规则不需要在房间或网络层复制判定。

- **足球中毒**：吃掉足球的单位进入绿色中毒状态，倒计时从 3 开始。新感染的当前回合不扣倒计时；此后每经过该棋子阵营自己的一个回合（包含超时），倒计时减 1，归零时棋子死亡且本方扣 1 血。吃掉中毒单位会传播并把倒计时重置为 3。中毒单位吃掉普通单位会先解毒；若目标本身是足球或中毒单位，则立即再次感染并重置。毒死的棋子计入对方吃子列表。
- **蛇**：每方原有 3 只猫中的 1 只替换为蛇，总棋子数仍为 16。蛇攻击正交相邻的敌方明子时，沿攻击方向将目标向后推一格，并移动到目标原格；后方越界或已有任意棋子时，目标无法后退并被蛇吃掉。大象免疫蛇的攻击。其他棋子攻击蛇时，把蛇按猫的强度处理；蛇把足球推出去不触发中毒，只有实际吃掉时才触发。
- 重复局面签名包含启用的规则产生的棋面状态（包括中毒倒计时和蛇击退后的站位），因此禁着判断与真实结算一致。

## 3. 联机架构

### 3.1 单端口服务

Node.js 服务监听 `0.0.0.0`，同一端口同时提供：

1. 静态网页、样式和 SVG 棋子资源；
2. `/api/health` 健康检查；
3. `/ws` WebSocket 实时连接。

默认端口为 `4173`，可通过 `PORT` 环境变量覆盖。启动时输出 `localhost` 与可用的局域网 IPv4 地址。

### 3.2 权威服务器

客户端只发送意图（创建房间、加入、准备、翻子、移动、认输等），服务器负责全部规则判断、随机洗牌、倒计时和胜负结算。客户端永远不提交棋子身份、血量或回合结果，因此局域网玩家无法通过篡改网页直接作弊。

为防止查看暗子，服务器为每个连接生成“玩家视图”：未翻开的格子只下发 `hidden: true`，真实棋子类型和阵营只保存在服务端。

### 3.3 房间模型

- 大厅维护房间列表；房间数量不设程序上限。
- 房间字段：房间号、名称、房主、两名玩家、无限观战者、初始血量、AI 版本、扩展规则集、状态（等待/进行中/已结束）。
- 加入者可明确选择“加入对局”或“观战”。普通加入会在有空位时占据玩家席位；主动观战始终进入观战席。两个玩家席位均满时，普通加入也自动成为观战者。
- 观战者数量不设上限，实时接收脱敏后的棋盘、倒计时、动画事件和吃子历史；有空位且未开局时可主动入座。
- 只有房主可在双方未准备时修改血量；两名玩家都确认准备后自动开始。
- 房主可在等待阶段添加或移除一个 AI 玩家来填补空位。AI 自动准备，真人确认后即可开局。
- 玩家离开等待中的房间时直接释放席位；房主离开后房主身份顺延给下一名玩家或观战者。
- 所有房间变化都实时广播到大厅，不依赖刷新。

### 3.4 会话、重连与时间

- 浏览器首次打开时生成随机会话令牌，保存在 `sessionStorage`；安全上下文优先使用 `crypto.randomUUID()`，局域网 HTTP 等非安全上下文自动降级到 `crypto.getRandomValues()` 生成 UUID v4，刷新页面仍使用同一身份。
- WebSocket 断开后客户端指数退避重连；服务端为对局玩家保留 20 秒。
- 服务端记录绝对截止时间 `turnDeadline`，每秒广播剩余秒数，并在服务端执行超时换手，避免不同设备计时漂移。
- 每条操作带当前对局版本号；过期、重复或非当前玩家的操作会被拒绝并返回可读错误。

### 3.5 AI 玩家

- AI 完全运行在权威服务器中，不通过伪造浏览器客户端操作，因此和真人遵守同一套合法走法与回合规则。
- 房主可在准备前选择 `V1` 或 `V2`，默认 `V2`。版本会跟随 AI 席位、对局动作诊断和历史记录保存；开局后禁止切换。
- **V1** 完整保留原版“SO-ISMCTS + 无暗子终局迭代加深 alpha-beta + 战术估值”，作为稳定基线和兼容选项。
- **V2** 是并行混合引擎。经典增强通道复用 V1 的成熟模拟统计，并对候选走法按剩余棋池的精确类型/数量做一层机会结果枚举和对方强制应手安全复核；概率通道搜索真正的公开信念状态，只有模拟执行翻棋时才从剩余多重集合抽样该格，不提前给其他暗子分配身份。因此概率通道不会产生“本次搜索已经知道以后会翻出什么”的策略融合。
- 两版 AI 都接收当前规则 ID 和规则修改后的棋子表，并调用与服务器相同的合法动作、蛇击退、毒素传播/解毒/倒计时模拟。估值会折损即将毒发棋子的价值，搜索能够比较解毒、传播、击退和堵住退路后的真实后果，不会按基础规则误判。
- 搜索输入只包含与真人相同的公开信息。所有未翻棋子在进入 AI 引擎前都被替换为无类型的暗子占位符，绝不把服务器保存的真实身份交给搜索线程。
- 两版都会从固定初始棋子表中扣除所有已翻开棋子和吃子列表，得到每种颜色/类型仍可能藏在场上的数量。V1 每次迭代从该多重集合生成一盘公开信息一致的假设；V2 概率通道延迟到翻棋动作发生时才抽样。已吃掉数量、已翻开数量、暗子总数与剩余类型概率都会直接影响决策。
- 信息集树按“公开棋盘 + 回合 + 血量 + 吃子历史 + 重复局面历史”建立节点；不同暗子采样共享同一公开决策统计，而翻开后的具体结果进入各自的新信息集，避免把某一次假设当成确定事实。
- V2 概率树采用 UCT + 渐消启发式偏置，并把搜索均值与隐式极小极大值混合。模拟在有限深度提前终止，使用连续质量分而不是只记胜/负；叶子会继续搜索强制吃子序列，降低水平线效应。研究中的四项暗棋 MCTS 增强——提前终止、隐式极小极大备份、质量型回报和 progressive bias——均进入了 V2 的设计。
- 当场上不再有暗子时，V2 使用迭代加深 alpha-beta、吃子静态延伸、置换表最佳着排序，以及带 `exact/lower/upper` 标记的安全置换表；V1 的旧终局引擎原样保留。
- V2 估值依次考虑血量差、动态克制关系、立即可吃子、悬挂棋子、机动性、中心控制、足球有效射线及临界血量。棋子价值会随对方尚存/可能藏有的克制子变化；因为吃任何一子都只扣一格血，安全制造连续伤害比固定名义等级更重要。
- 搜索预算为每步 15 秒，并由 15 秒硬上限截断；到时聚合所有线程已经得到的统计，保留当前最佳合法走法。主服务器线程不参与长搜索，仍可持续处理观战、重连和倒计时。
- 整台服务器只创建一个固定大小的 AI 搜索池：使用 `os.availableParallelism()` 取得本机可用逻辑线程数，再向下取一半（至少 1 个）。当前机器可用 8 个逻辑线程，因此固定使用 4 个搜索 Worker，不会因为房间增加而无限创建线程。
- 每个 AI 局面被拆成约 400ms 的搜索时间片。调度器每次把空闲 Worker 分给当前占用线程最少的房间：只有一个 AI 同时思考时会用满整个池；多个 AI 房间并发时会在一个时间片内重新均分。V2 暗棋搜索约 7/8 时间片运行经典增强通道、1/8 运行公开信念机会节点通道，以稳定基线为主、用独立概率模型校正确定化偏差，最后按根访问量和质量合并；完全信息残局则把根走法分片给不同 Worker，再合并各自最深且完整完成的 alpha-beta 结果。
- 真人回合开始后，AI 立即进入“预判”模式。搜索根节点是当前真人的信息集，树策略把真人视为最小化方，并分别记录真人每种可能移动、吃子或翻子结果之后的 AI 回合节点。翻子预判仍只对剩余棋池做概率采样，不会从服务器读取暗子真实身份。
- 真人实际操作后，服务端用新的“公开棋盘 + 回合 + 血量 + 吃子历史 + 重复局面历史”信息集键收割完全匹配的预判分支。该分支累计的访问次数、均值和先验会作为 AI 正式搜索的初始统计继续聚合；未匹配的假设分支直接丢弃。
- 预判从真人回合开始计时，到真人落子或超时为止，不消耗 AI 自己回合的 15 秒。AI 正式回合仍获得完整 15 秒预算；对局记录会分别保存预判模拟次数和本回合模拟次数，便于确认复用确实发生。
- 搜索任务绑定棋局版本和当前阵营；任务完成前若对局结束、加载测试棋局、换手或房间销毁，任务会立即取消，尚在执行的短时间片结果会被忽略，固定 Worker 随后回到线程池服务其他房间。
- AI 不占用房主身份，不需要断线重连。真人离开且房间不再有真人玩家时，AI 会随房间一起清理。

### 3.6 历史对局持久化与回放

- 仅已结束的对局进入历史库；房间等待状态和服务器停机时尚未结束的棋局仍属于临时内存状态。
- 每局保存房间名、起止时间、初始血量、双方名称/阵营/AI 标记、胜负原因，以及“初始画面＋每次翻棋、移动、吃子、超时或结束动作后的画面”。
- 回放帧只包含当时的公开信息：暗子统一保存为 `{ hidden: true }`，不会把服务器掌握但玩家从未翻开的真实棋子写入历史文件。
- 默认文件为 `data/game-history.json`，也可由 `HISTORY_FILE` 指定。写入先落到同目录临时文件，再以原子重命名替换正式文件；损坏的现有文件会阻止加载而不是被静默覆盖。
- 列表查询只传输摘要，用户选择某一局后才传输完整帧序列。客户端可手动前后步、拖动进度、播放/暂停，并用统一的 0.3–3.0 秒间隔回放所有步骤。

研究基线：

- Cowling、Powley、Whitehouse，Information Set Monte Carlo Tree Search（IEEE TCIAIG, 2012）：隐藏信息游戏应搜索信息集而不是把一个确定化棋盘当成真实状态。
- Yen 等，Monte Carlo Tree Search in Chinese Dark Chess（GPW 2011）：针对中国暗棋修改 MCTS 后获得显著提升，并在 2011 TCGA 赛事中全胜。
- Chen、Shen、Hsu，Chinese Dark Chess（ICGA Journal, 2010）：翻子策略、阶段切换和中终局评估函数是暗棋程序的核心问题。
- Hsueh、Wu、Tseng、Yen、Chen，An analysis for strength improvement of an MCTS-based program playing Chinese dark chess（2018）：在 DARKKNIGHT 上分别验证提前终止、隐式极小极大、质量型回报和 progressive bias，其中 progressive bias 对原版胜率达到 82.10%。
- Silver、Veness，Monte-Carlo Planning in Large POMDPs（NeurIPS 2010）：用黑盒模拟器对信念状态进行在线蒙特卡洛规划，为 V2“不预先确定化未触发暗子”的机会节点通道提供方法依据。

## 4. 代码分层

```text
server/
  game-engine.mjs   纯规则、棋盘、吃子、回合与胜负（无网络依赖）
  rule-system.mjs   扩展规则注册表与通用钩子调度
  rules/*.mjs       足球中毒、蛇等可独立增删的规则模块
  history-store.mjs 历史记录校验、原子磁盘持久化与查询
  ai-engine.mjs     完整保留的 V1 信息集 MCTS、终局搜索和估值
  ai-engine-v2.mjs  V2 公开信念机会树、战术复核、动态估值与强化终局
  ai-versions.mjs   V1/V2 版本校验与搜索分发
  ai-scheduler.mjs  固定半核线程池、时间片公平调度与并行结果聚合
  ai-worker.mjs     可复用的后台搜索线程入口
  room-manager.mjs  大厅、房间、AI 任务、座位、准备、重连和广播视图
  server.mjs        HTTP、WebSocket、静态资源、局域网地址
public/
  index.html        单页入口
  app.js            客户端状态、消息处理、交互和渲染
  uuid.mjs          安全与非安全上下文兼容的会话 ID 生成
  captured.mjs      吃子统计的小到大排序与同类聚合
  styles.css        严格复刻的响应式舞台与动画
  pieces/*.svg      暗子、大象、老虎、狼、狗、猫、蛇、老鼠、足球
data/
  game-history.json 运行后生成的历史对局记录
tests/
  game-engine.test.mjs
  room-manager.test.mjs
  server.integration.test.mjs
```

规则引擎保持纯函数风格，网络层不重复业务判断；这样关键规则可快速、确定性地自动测试。

## 5. 通信协议（摘要）

客户端消息：

- `hello`：昵称和会话令牌；
- `create_room`、`join_room`、`leave_room`；
- `join_room` 可带 `asSpectator` 显式观战；
- `take_seat`、`add_ai`、`remove_ai`、`set_ready`、`set_health`、`set_ai_version`、`set_rule`；
- `flip`、`move`（均含对局版本号）；
- `resign`、`rematch`、`ping`。
- `history_list`：请求所有历史摘要；`history_get`：按对局 ID 请求完整回放。

服务端消息：

- `welcome`：当前身份与服务器配置；
- `lobby`：公开房间列表；
- `room`：所在房间完整可见状态；
- `game`：经过暗子脱敏后的对局状态；
- `notice`、`error`：操作结果和错误提示；
- `pong`：延迟检测。
- `history_list`：历史摘要列表；`history_record`：单局完整公开回放帧。

## 6. 测试与交付标准

### 6.1 自动测试

- 强弱矩阵、同级互吃、老鼠/大象特例、老鼠不能吃足球。
- 所有边界移动、斜走、跨格、己方占位和暗子限制。
- 足球恰好一个炮架、零/多个空格、无炮架和多个炮架的情况。
- 翻子、吃子扣血、0 血结算、认输、超时过轮、版本号增长、同一局面第 4 次出现时禁着且不改变棋局。
- 规则注册、房主切换权限与准备锁定；中毒传播、解毒、三次己方回合毒发、超时计数；蛇的击退、阻挡/边界吃子、大象免疫与猫级防守；AI 模拟和历史回放保持同一规则集。
- 多房间创建/加入/显式无限观战/离开、AI 增删与自动走子、准备开局、血量设置权限、房主转移、断线重连。
- AI 输入暗子脱敏、V1 完整保留、V2 版本选择、剩余棋池计数、机会节点不确定化其他暗子、概率树复现、必胜/避险战术、吃子静态延伸、足球远程威胁、真人回合预判分支命中与复用、预判时间不侵占正式 15 秒、后台任务过期丢弃，以及固定半核线程池的 V2 混合通道和多房间公平调度。
- 吃子列表按鼠、猫、狗、狼、虎、象、足球排序，同类以 `×N` 聚合；16 枚全满时仍不裁切。
- 局域网非安全 HTTP 环境缺少 `crypto.randomUUID()` 时仍能生成服务端接受的会话 ID。
- 启动真实随机端口，通过 HTTP 和两个 WebSocket 客户端完成一局关键流程。
- 结束对局只写入一次；历史列表与完整记录协议可用；关闭并重建服务器后仍能从同一文件读取并回放。

### 6.2 内置浏览器手动测试

- 分别以真人、AI 和多个观战者视角打开标签页，创建/加入/退出/再次加入房间。
- 修改血量，双方准备并进入游戏。
- 实际完成翻子、选子、合法移动、非法移动提示、普通吃子、足球隔子吃子、回合倒计时与超时过轮。
- 测试认输、胜负弹层、再来一局、刷新重连和断线提示。
- 逐项检查执子上浮、棋子位移、吃子冲击、翻面、最后 5 秒强调和侧边吃子列表。
- 分别以窄手机、宽手机和桌面视口截图检查；重点对照棋盘比例、棋子造型、血条、按钮、文字层级和触摸命中区。

### 6.3 完成定义

- `npm test` 全部通过，`npm start` 可直接运行。
- 同一局域网中的设备能通过启动日志给出的地址进入。
- 两名玩家在无需刷新页面的情况下能完成从大厅到胜负结算再到重赛的完整闭环。
- SVG 棋子、棋盘与整体排版在常见手机比例下与四张示例图保持同一视觉语言，不出现溢出、遮挡或不可点击区域。
